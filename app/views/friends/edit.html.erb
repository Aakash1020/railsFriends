


<div class="card w-50 shadow-lg p-3 mb-5 bg-body rounded">
  <div class="card-header">
    <h1>Editing Friend</h1>
  </div>
  <div class="card-body">
	<%= render 'form', friend: @friend %><br/>

	<%= link_to 'Back', friends_path, class:"btn btn-secondary" %>
	<%= link_to 'Delete', friend_path, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-outline-danger" %>
  </div>
</div>